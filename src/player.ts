import 'phaser';

export class Player {
    x: number;
    y: number;
    image: Phaser.Physics.Arcade.Image;
    angularVelocity: number;

    constructor(scene: Phaser.Scene) {
    }

    create(): void { }
    update(): void { }
}